// Placeholder for Mailgun email service
const formData = require('form-data')
const Mailgun = require('mailgun.js')

class EmailService {
  constructor() {
    // TODO: Initialize Mailgun client
  }

  async sendImageEmail(imageBuffer, recipient) {
    // TODO: Send email with image attachment
  }
}

module.exports = EmailService
